# Tasks — Lightning Detection in Gallery

- [x] Add `google_mlkit_image_labeling` to `pubspec.yaml` (`fvm flutter pub add google_mlkit_image_labeling`) and confirm `android/app/build.gradle.kts` has `minSdk ≥ 21`.
- [x] Add the `lightning_confidence_threshold` setting to `SettingsManager` (key, `min/maxLightningThreshold` consts, `_lightningThreshold` signal default `0.55` clamped on load, getter, signal getter, `setLightningThreshold` clamp+persist), following the `_maxStrikeDistanceKm` pattern.
- [x] Settings page: add a "Lightning sensitivity" `Slider` `ListTile` (in a `SignalBuilder`) bound to the threshold, value shown as a percentage with an explanatory subtitle.
- [x] Create `lib/utils/volume_key_dispatcher.dart`: singleton owning the `com.wisp.lightingcamera/volume_keys` channel with a listener stack (`subscribe`/`unsubscribe`, top listener wins).
- [x] Migrate `camera_page.dart` to the dispatcher (subscribe in `initState`, unsubscribe in `dispose`, keep `_volumeButtonsEnabled`/visibility/debounce logic inside the listener); delete its direct `MethodChannel` handler.
- [x] Create `lib/camera/lightning_detection_service.dart`: `lightningDetectionService` singleton with `confidences` map signal (every scanned frame, keyed by sequence number), `scannedCount`/`totalCount`/`isScanning` signals, `isHit`/`hitsByConfidence` derived from `lightningThresholdSignal`, generation-based `reset()`, and `whenScanComplete()`.
- [x] Implement YUV420 → NV21 conversion (row-stride-aware Y copy, V/U interleave handling pixel stride 1 and 2) and `InputImage.fromBytes` metadata incl. rotation derived from frame orientation + lens direction; pin the labeler's confidence floor at/below `minLightningThreshold`.
- [x] Implement the sequential `scan()` loop: label each frame, record its `Lightning` confidence (or `0.0`), count errors as scanned, log failures with `Fimber.e`, abort cleanly on generation change.
- [x] Gallery: start `scan(images)` in `initState`; call `reset()` in `dispose` and `_returnToCamera()`.
- [x] Gallery: add the `_LightningSection` band above the grid — `SignalBuilder` over `hitsByConfidence(images)`, header "Lightning (N)", horizontal strip of thumbnails (most confident first) with bolt + confidence caption, tap opens fullscreen; renders nothing when zero hits.
- [x] Gallery grid: amber border + top-left `Icons.bolt` badge on hit tiles via `SignalBuilder` (tracks confidences + threshold), coexisting with selection overlay/checkmark.
- [x] Gallery app bar: scan `LinearProgressIndicator` as `AppBar.bottom` while scanning (via `SignalBuilder`).
- [x] Gallery app bar: add an `Icons.tune` "Lightning sensitivity" toggle that reveals a compact threshold `Slider` row (shares the `AppBar.bottom` strip with the progress indicator), `SignalBuilder` on `lightningThresholdSignal`, `onChanged` → `setLightningThreshold`; reachable regardless of hit count.
- [x] Refactor `_saveAll` into shared `_saveImages(List<ImageWithMetadata>)` helper (permissions, convert-or-reuse, JPEG 95, snackbar); `_saveAll` delegates to it; unify the busy flag so concurrent saves are impossible.
- [x] Gallery app bar: add `Badge.count` + `Icons.bolt` "Save lightning (N)" button calling `_saveImages` with hit images; count/enabled state via `SignalBuilder`; disabled at zero or while saving.
- [x] Gallery volume flow: subscribe to dispatcher; first press opens the confirm dialog (states hit count, scan progress when running, zero-found wording); second press or on-screen confirm → `whenScanComplete()` → save hits → `_returnToCamera()`; cancel re-arms.
- [x] `fvm flutter analyze` clean (no new warnings — only pre-existing `withOpacity`/`Watch`/fullscreen-`await` issues remain).
- [ ] On-device check (manual): scan flags real lightning frames and the top section + badges appear progressively; the gallery sensitivity slider re-filters the section/badges/count live with no rescan, stays in sync with the settings-page slider, and survives restart; "Save lightning" saves only hit images; volume double-press saves and exits to a working camera page; camera volume shutter still fires before and after a gallery visit; system volume UI never appears.
