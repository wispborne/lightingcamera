# Tasks — Speed Up Gallery Loading

- [ ] `native/yuv_converter.c`: replace `convert_yuv_to_rgb` with `convert_yuv_to_rgb_scaled` (output-pixel loop, `y_row_stride` honored, `scale` subsampling, same fixed-point math and inline rotation applied to scaled dims).
- [ ] `lib/native/yuv_converter_ffi.dart`: update typedefs/lookup for the new symbol; named-parameter Dart API with `scale` defaulting to 1; output buffer sized `(w~/scale) * (h~/scale) * 4`.
- [ ] `lib/camera/image_converter.dart`: make `rotationFor(orientation, lens)` public returning normalized `0/90/180/270` (`-90` → `270`); add `thumbScaleFor(w, h)`; delete `_convertYuvToRgbDart`.
- [ ] `lib/camera/image_converter.dart`: replace `ProcessedImage` with `ProcessedFrame` (RGBA bytes + dims, cached `uiImage` future, `releaseBytes()`, `toImgImage()`, `dispose()`); delete the synchronous `processImage` path.
- [ ] `lib/utils/photo_exif.dart`: add isolate-sendable `JpegEncodeInfo` and `encodeJpgWithInfo(img.Image, JpegEncodeInfo)`; turn `encodeJpgWithMetadata` into a thin adapter over it.
- [ ] Create `lib/camera/yuv_conversion_pool.dart`: `yuvConversionPool` singleton, 2 lazy long-lived worker isolates, three-level priority queue (high/normal/low), request types rgba/jpeg/nv21, send-time plane copies (references while queued), id↔completer correlation, worker errors stringified and `Fimber.e`-logged on the main isolate, `cancelPending()`, dead-worker respawn; convert function injectable for host tests.
- [ ] Gallery grid: rename `_displayImages` → `_thumbs` (`Map<int, ProcessedFrame>`); on open, eagerly enqueue thumbnail conversion for every frame in index order at normal priority (`scale: thumbScaleFor(...)`); on completion guard `mounted && _generation == gen`, await `uiImage`, `releaseBytes()`, `setState`.
- [ ] Gallery grid: delete `_convertImageBatch` / `_convertSingleImage` / `_convertCameraImageToUIImage`, the `itemBuilder` conversion trigger, `_currentlyConverting` bookkeeping, and the lightning-strip conversion trigger (strip reads `_thumbs`); tiles show the dim-icon placeholder until their thumb lands (no spinner).
- [ ] Gallery deletion flows: after the `_generation` bump, `cancelPending()` and re-enqueue thumbnails missing under remapped indices; `dispose()` also calls `cancelPending()`.
- [ ] Fullscreen viewer: take `thumbs` (renamed); `_localConverted` holds full-res `ProcessedFrame`s requested at high priority; page shows the upscaled thumbnail (`BoxFit.contain`) until full-res lands, then swaps; keep ±1 prefetch; dispose full-res entries outside `index ± 2`.
- [ ] Saves (`_saveImages` + fullscreen `_saveImage`): per frame one pool request at `scale: 1` with `jpeg: JpegEncodeInfo` at high priority, 2–3 in flight, `Gal.putImageBytes` on completion; drop the convert-or-reuse block; permission flow, busy flag, counts, snackbars unchanged.
- [ ] Lightning detection: `scan()` gets NV21 bytes from the pool at low priority (packing loop moves into the worker); replace `_rotationFor`'s angle table with `ImageConverter.rotationFor`; generation guard and signals untouched.
- [ ] Update CLAUDE.md's Image Pipeline section to describe the pool-based pipeline (the `compute()` claim is stale).
- [ ] Host tests: `thumbScaleFor` boundaries (1080p→3, 720p→2, ≤360→1), `rotationFor` normalization (never negative, portrait-down cases), `encodeJpgWithInfo` EXIF parity with the `encodeJpgWithMetadata` adapter, pool priority ordering + `cancelPending()` with an injected fake convert function (the `.so` can't load on a Windows host).
- [ ] `fvm flutter analyze` and `fvm flutter test` clean.
- [ ] On-device check (manual): gallery with ~100 frames opens with tiles filling in progressively, no scroll jank, no per-tile spinners; fullscreen opens instantly (soft) and sharpens, swiping stays instant; save-all stays responsive and produces full-resolution JPEGs with intact EXIF/GPS; portrait-down captures render upright; lightning scan completes with live progress and unchanged hits; memory profiler shows gallery peak well below previous behavior.
